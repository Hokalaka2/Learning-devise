<!DOCTYPE html>
<html>
  <head>
    <title>Membership</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if flash[:notice] %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <% if flash[:alert]%>
      <p class="alert"><%= alert %></p>
    <% end %>

    <nav class = "navbar navbar-expand-sm navbar-light bg-light">
        <ul class="navbar-nav ml-auto">
        
          <% if user_signed_in? %>
            <li class="nav-item">
              Profile
            </li>
            <li class = "nav-item">
              <a class= "navbar-link"><%= link_to 'Log Out', destroy_user_session_path, method: :delete %></a>
            </li>
          <% else %>
            <li class = "nav-item">
              <a class= "navbar-link"><%= link_to 'Sign In', new_user_session_path %></a>
            </li>
            <li class = "nav-item">
              <a class= "navbar-link"><%= link_to 'Sign Up', new_user_registration_path%></a>
            </li>
          <% end %>
        </ul>
    </nav>
    <%= yield %>
  </body>
</html>
